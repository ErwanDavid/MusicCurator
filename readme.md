### Music Curator

## Motivation
Tool that scan a folder and extract mp3 metadata to build a music database. It can be used to curate a music collection, find duplicates, and generate playlists.


## Usage

```bash
pip install eyed3
python musicCuratorNg.py --path /path/to/your/music/folder
```